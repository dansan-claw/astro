info:
  app:
    name: "Bot"
    description: "The main bot Discord application."
    version: "1.0.0"

management:
  metrics:
    export:
      prometheus:
        enabled: true
  endpoints:
    web:
      cors:
        allowed-origins: "*"
        allowed-methods: GET, POST
      exposure:
        include:
          - health
          - info
          - metrics
          - prometheus

logging:
  level:
    root: ${LOG_LEVEL:INFO}

application:
  features:
    premium-restrictions: ${PREMIUM_RESTRICTIONS:false}

discord:
  application:
    token: ${BOT_TOKEN}
    bot-id: ${BOT_ID:788708323867885568}

bigquery:
  enabled: ${BIGQUERY_ENABLED:false}
  dataset-name: ${BIGQUERY_DATASET_NAME:astro_local}

chargebee:
  api-key: ${CHARGEBEE_API_KEY}
  webhook-token: ${CHARGEBEE_WEBHOOK_TOKEN}
  site-name: ${CHARGEBEE_SITE_NAME}
kube:
  lifecycle-authorization: ${KUBE_LIFECYCLE_AUTHORIZATION}
premium:
  api:
    base-url: ${PREMIUM_API_BASE_URL:http://localhost:9001}
    origin-url: ${PREMIUM_API_ORIGIN_URL:http://localhost:3000}
    auth: ${PREMIUM_API_AUTH:dev-premium-api-auth-token}

io:
  influx:
    bucket: ${INFLUX_BUCKET:astro-logs}
    org: ${INFLUX_ORG:astro-org}
    token: ${INFLUX_TOKEN:astroverysecuretoken}
    url: ${INFLUX_URL:http://localhost:8086}
  mongo:
    connection-string: ${MONGO_CONNECTION_STRING:mongodb://localhost:27017}
    database: ${MONGO_DATABASE:AstroDev}
  redis:
    uris: ${REDIS_URIS:redis://localhost:6379}
    cluster: ${REDIS_CLUSTER:false}

sentry:
  dsn: https://6b38b9b99e0d693f67b3355d0c4c83f6@o4506570641702912.ingest.sentry.io/4506575860203520
  # Set traces-sample-rate to 1.0 to capture 100% of transactions for performance monitoring.
  # We recommend adjusting this value in production.
  traces-sample-rate: 1.0