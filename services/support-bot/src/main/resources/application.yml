info:
  app:
    name: "Support bot with an API"
    description: "A Discord bot that handles the support server of Astro, includes an API for reacting to various events, mainly premium."
    version: "1.0.0"

server:
  port: ${PORT:9002}


management:
  endpoints:
    web:
      cors:
        allowed-origins: "*"
        allowed-methods: GET, POST
      exposure:
        include:
          - health
          - info
          - metrics
          - prometheus
  endpoint:
    prometheus:
      enabled: true
    health:
      probes:
        enabled: true
      group:
        readiness:
          include:
            - readinessState
        liveness:
          include:
            - livenessState
  prometheus:
    metrics:
      export:
        enabled: true


##############
### SHARED ###
##############
logging:
  level:
    root: ${LOG_LEVEL:INFO}


sentry:
  dsn: https://fd05f462a5f47e39c3cd927fc63a3021@o4506708022263808.ingest.sentry.io/4506708030128128
  # Set traces-sample-rate to 1.0 to capture 100% of transactions for performance monitoring.
  # We recommend adjusting this value in production.
  traces-sample-rate: 0.25

bigquery:
  enabled: ${BIGQUERY_ENABLED:false}
  dataset-name: ${BIGQUERY_DATASET_NAME:astro_local}

chargebee:
  api-key: ${CHARGEBEE_API_KEY}
  webhook-token: ${CHARGEBEE_WEBHOOK_TOKEN}
  site-name: ${CHARGEBEE_SITE_NAME}
kube:
  lifecycle-authorization: ${KUBE_LIFECYCLE_AUTHORIZATION}
support:
  bot:
    base-url: ${SUPPORT_BOT_BASE_URL:http://localhost:9001}
    origin-url: ${SUPPORT_BOT_ORIGIN_URL:http://localhost:3000}
    auth: ${SUPPORT_BOT_AUTH:dev-premium-api-auth-token}

io:
  influx:
    bucket: ${INFLUX_BUCKET:astro-logs}
    org: ${INFLUX_ORG:astro-org}
    token: ${INFLUX_TOKEN:astroverysecuretoken}
    url: ${INFLUX_URL:http://localhost:8086}
  kmongo:
    connection-string: ${KMONGO_CONNECTION_STRING:mongodb://localhost:27017}
    database: ${KMONGO_DATABASE:AstroDev}
  redis:
    cluster: ${REDIS_CLUSTER:false}
    uris: ${REDIS_URIS:redis://localhost:6379}


#####################
### END OF SHARED ###
#####################

discord:
  application:
    bot-id: ${DISCORD_APPLICATION_ID:1176957574332026940}
    token: ${DISCORD_APPLICATION_TOKEN}
    command-guilds: [700607091391594567]
    whitelisted-guilds: [700607091391594567]
    entitlements-bot-id: ${ENTITLEMENTS_BOT_ID:715621848489918495}
    entitlements-bot-token: ${ENTITLEMENTS_BOT_TOKEN}
    guild-id-for-premium-role: ${GUILD_ID_FOR_PREMIUM_ROLE:700607091391594567}
    premium-sku-id: ${PREMIUM_SKU_ID:1096107722115661934}
    premium-role-id: ${PREMIUM_ROLE_ID:761283474241093682}

shard:
  manager:
    total-pods: ${SHARD_MANAGER_TOTAL_PODS:1}
    total-shards: ${SHARD_MANAGER_TOTAL_SHARDS:1}